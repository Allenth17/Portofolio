package com.allenth17.portofolio.theme

import androidx.compose.ui.graphics.Color

// ── Background ─────────────────────────────────────────────────────────────
val BackgroundLight   = Color(0xFFFBF9F6)   // Soft latte cream / warm sand white
val SurfaceLight      = Color(0xFFFFFFFF)   // Pure white cards
val SurfaceVariant    = Color(0xFFF2ECE0)   // Latte cream / warm beige

// ── Text ───────────────────────────────────────────────────────────────────
val TextPrimary       = Color(0xFF3E2723)   // Deep espresso brown — strong, readable
val TextSecondary     = Color(0xFF7A685D)   // Warm mocha/latte grey
val TextOnAccent      = Color(0xFFFFFFFF)   // White on colored buttons

// ── Accent / Brand ─────────────────────────────────────────────────────────
val AccentPurple      = Color(0xFF8D5B4C)   // Soft clay / terracotta pastel brown — primary
val AccentLavender    = Color(0xFFC3A387)   // Soft warm sand / beige — secondary
val AccentPeach       = Color(0xFFDCA482)   // Soft peach orange pastel — accent
val AccentMint        = Color(0xFF9EA990)   // Soft sage green pastel — success / CTA

// ── Glass / Card ───────────────────────────────────────────────────────────
val GlassBackground   = Color(0xE6FFFDFB)   // Semi-transparent warm cream card
val GlassBorder       = Color(0xFFEDE5D9)   // Very soft warm sand/beige border

// ── Legacy alias (keep existing code compiling) ────────────────────────────
val BackgroundDark    = BackgroundLight     // redirect
val AccentIndigo      = AccentLavender      // redirect

