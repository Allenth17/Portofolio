package com.allenth17.portofolio.theme

import androidx.compose.ui.graphics.Color

// ── Background ─────────────────────────────────────────────────────────────
val BackgroundLight   = Color(0xFFF7F5FF)   // Soft lavender-white — warm & airy
val SurfaceLight      = Color(0xFFFFFFFF)   // Pure white cards
val SurfaceVariant    = Color(0xFFF0ECFF)   // Lavender tinted surface

// ── Text ───────────────────────────────────────────────────────────────────
val TextPrimary       = Color(0xFF1E1B4B)   // Deep indigo — strong, readable
val TextSecondary     = Color(0xFF6B7280)   // Warm grey
val TextOnAccent      = Color(0xFFFFFFFF)   // White on colored buttons

// ── Accent / Brand ─────────────────────────────────────────────────────────
val AccentPurple      = Color(0xFF7C3AED)   // Vivid violet — primary brand
val AccentLavender    = Color(0xFFA78BFA)   // Soft lavender — secondary
val AccentPeach       = Color(0xFFFB7185)   // Coral-pink — energetic pop
val AccentMint        = Color(0xFF34D399)   // Fresh mint green — success / CTA

// ── Glass / Card ───────────────────────────────────────────────────────────
val GlassBackground   = Color(0xCCFFFFFF)   // Semi-transparent white card
val GlassBorder       = Color(0xFFE5E1FF)   // Lavender border

// ── Legacy alias (keep existing code compiling) ────────────────────────────
val BackgroundDark    = BackgroundLight     // redirect — no longer dark
val AccentIndigo      = AccentLavender      // redirect
